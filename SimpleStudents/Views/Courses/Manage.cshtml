@model IEnumerable<SimpleStudents.Domain.Course>

@{
    ViewBag.Title = "Courses management";
}
<div class="courses">
    <table>
        
            @foreach (var course in Model)
            {
                <tr>
                    
                    <td>@(course.Name)</td>
                    <td>@(course.Teacher?.FirstName)</td>
                    <td>@(course.Teacher?.LastName)</td>
                </tr>
            }
        
    </table>
</div>
    
<form method="post" action="">
    <p/>
    <h2>Input course</h2>
    <input type="text" name="Name"/>

    <input type="submit" value="Отправить"/>
</form>

@*@using (Html.BeginForm())
{
    <p>Enter course name: @Html.TextBoxFor(x =>x.Name)</p>
    <p/>
    <input type="submit" value="Add course to database"/>
}*@